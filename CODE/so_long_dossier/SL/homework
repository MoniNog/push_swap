DONE		rapetissir images
DONNNNNE :)		reflechir a comment quitter panier sans quil disparaisse
DONE		pouvoir fermer la fenetre en cliquant sur x
DONE		changer les touches haut bas gauche droite par asdw
DONE		image win + grande
DONE		print nombre de mouvements sur le shell
OK		La gestion de la fenêtre doit rester fluide (changer de fenêtre, la réduire, etc.)
DONE	liberer memoire
DONE	fin du jeu

DONE	quand le jeu est gagne manque de ne pas pouvoir reprendre le jeu

DONE		comment faire pour que le nombre de deplacement simprime quune seule fois ?

		//verifier dans les checkers pouruqoi le 2eme appel ne fonctionne pas. 
		//modification de la map une fois checke ?



//			ELISE :
	// map.render// variable de type t_map
	// map->render// pointeur vers render (path)

	// y = 0;
	// while(y <= map->height)
	// {
	// 	x = 0
	// 	while(x <= map->width)
	// 	{
	// 		x++;
	// 	}
	// 	y++;
	// }


	void	move_player(t_map *map, int keypress)
{
	//int dest_x;
	//int dest_y;
	//if keypress => modifier destination
	//if dest == '1' => return ()pas de modif
	//if dest == 'C' => coin-- 
	//après les if : player = '0', dest = 'P'
	

	Repenser checkers
	eventuellement (ca marche mais ca peut etre mieux) :
		map4 plusieur ou pas de p ou e -> gerer checker et startetexitpos
	map depasse la fenetre